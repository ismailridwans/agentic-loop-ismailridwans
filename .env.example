# Copy to .env and fill in. .env is gitignored; THIS file is not — it gets
# committed, so never put a real key here.
GROQ_API_KEY=

# Optional overrides. Defaults are in agent/llm.py and move to config.yaml in M3.
# GROQ_MODEL=openai/gpt-oss-120b
# GROQ_FAST_MODEL=openai/gpt-oss-20b
